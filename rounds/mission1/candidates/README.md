# Mission 1 story candidates (for blind review)

Four independent candidate scientific stories for the BIOC1600 glutamate-aptamer poster question. Each dossier was synthesized from one isolated trio (steelman, red team, independent) after those agents returned. They are **not** a ranking.

Blind reviewer instructions:

- Read all four dossiers.
- Read the ledger and reviews listed in `evidence_packet.md`.
- Score each candidate on the locked rubric in `.cursor/rules/scoring-rubric.mdc`.
- Do not read `reports/nightly_summary.md`, `state/scoreboard.json`, `analysis/candidates/theses/`, `poster/theses.md`, `rounds/03`, `rounds/04`, or any file that ranks prior theses T1–T5.
- Do not treat this README, `protocol.md`, or sealed lane notes as a preferred winner.
- Abrantes millimolar ELONA Kd is **not** a ledger number. Tokens `10.3` / `25.1` in API JSON were previously identified as funder IDs. Do not promote millimolar ELONA Kd.

Occupancy table rows are SIMULATION/BOUND. No glutamate aptamer `kon`/`koff` is in the ledger (C007).
