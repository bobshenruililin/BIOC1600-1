---
cursor:
  subagentId: "bc-55300180-273d-5a2d-b39e-840e490f6a0d"
---

# Mission 3 — late scientific challenge (Opus)

Late scientific challenge of the Mission-3 communication package over frozen science. **Communication recommendations only.** Mission 2 not reopened. No science reopen isolated. No PR. Astra not invoked. Prototypes not overwritten. No numbers, citations, or locators invented. Nothing labeled FINAL. No renderer used — every face was scored from **SVG text** (`<text>`, `<tspan>`, `aria-label`; XML comments not scored).

Independent of Fable: no Fable report was read, requested, or waited on. Written before/without whole-object integration output.

**Freeze (authority).** `https://github.com/bobshenruililin/BIOC1600-1` `main` @ `3fa11240c85df9ebac58dcc6879494911968f75c` — `state/current_thesis.md` (D_revised, gate **REVISE**, QUANTITATIVE FLAGSHIP **NONE**, **U2** locked), `state/high_value_unknowns.md`, `state/claims.csv`, `research/evidence/core_evidence.csv`.

**Governing.** `docs/mission3-constitution.md` (store).

**Scored objects (8 faces).** `hybrid-hero-v3-fidfix2.svg`, `b2-maps.svg`, `b2-spikes.svg`, `b2-clocks.svg`, `b3-3q-traps.svg`, `b3-3h-regimes.svg`, `b3-why-layers.svg`, `u2-close.svg`. **`b3-why-layers-fidfix.svg` landed during this challenge and was scored as well** — it repairs all three copy FAILs from `internal/mission3/tests/fidelity-b3-why-layers.md` (see M8); the parent stays on disk, unoverwritten, and is no longer the face to defend.

**Oral.** `internal/mission3/oral-defense-kit.md` plus `internal/mission3/tests/hostile-answers-round2.md` (present) and the round-2 attack file.

**Already bound — not re-litigated here.** Quote/*p* MINOR; 1.8 nM parked and visible; Leg A settling is the time IV; no `≠` in the finding row; no empty PaC Kd well; no `protocol_clocks.svg`; no untraced dark/light Hz; recognition-first KILL not grafted. Where an item below touches a bound object, it is scoped to a different string and says so.

---

## Verdict

**FATAL: none.**

**INTERNAL RC: none.** Five IMPORTANT items are open, and four constitution release criteria do not actually hold (visible biochemistry, rebuildable figures, effective hierarchy, auditable evidence chains — table at the end). Gate stays **REVISE**. Not group-final.

The package is freeze-faithful in the places it was attacked hardest. Its remaining weakness is not a smuggled claim; it is that **the scored set is almost entirely composed of refusals**. Eight faces say what a number is not, what a card is not, what an experiment does not name. Across all eight, the word *aptamer* appears **zero** times, and no face carries molecular recognition, transduction, folding, selectivity, or SELEX. A BIOC1600 assessor can leave this board convinced the group audited a thesis carefully without ever seeing the biochemistry the course examines. That is the largest correctable gap, and it is correctable inside the freeze.

---

## FATAL — none

Searched for, and did not find, on any scored face or in either oral file:

- either S066 tissue leg spoken or stamped as identified / MEASURED glutamate (including Hu's "successful detection" sentence as a result);
- occupancy θ recovered, overlaid, or computed; θ(25 nM)≈0.93; slope ratio `57.11/11.86` as occupancy or identity;
- 1.8 nM as this PaC probe's molecular `Kd`, or the parked chip covered/removed (covering it would have been FATAL; it is intact and legible);
- Thesis E ("the current is not glutamate" / ephys disconfirms glutamate) as a finding — guards present on hero and `b2-spikes`; the why-layers audibility route is closed on the fidfix sibling (M8);
- Nyquist, PR #24 as accepted, 1.2 ms on the clocks face, or `protocol_clocks.svg` pointed at;
- 0.3 pM as an Ames or tissue LOD; 32 pM as the matrix-matched serum number; 12 µM as a Hu remeasurement;
- a manufactured quantitative flagship (no face titles occupancy, Kd, LOD, or tissue-[Glu]);
- in vivo sensing; S002 confused with S066;
- Ch. 7 "steady-state [Glu]" as a licensed MEASURED reading of Fig. 6.13;
- U1 substituted for the locked U2 as the next experiment;
- FINAL / submission-ready / print-approved / Astra-approved labels.

Numbers on the faces were checked against ledger rows, not against other agents' summaries: `11.86 %/decade` / Fig. 6.7B (E047), `57.11 %/decade` / Fig. 6.9A (E048), `41.6%` blank noise and `10 nM–10 µM` (C030/E044), `14 s` (E045/C034), `~1 min` (E046/C031), `10 min` plateau after a 10 nM step (C028), `0.3 pM` with lowest calibrant 1 nM (C029), `1.8 nM` `EC50` (C005), `12 µM` (C001 and, separately, C021), `~25 nM` (C012), `~1.1 mM` / `~1.2 ms` (C011). All match. No invented numeral appears on any scored face.

No fatal factual contradiction was isolated, so **no Mission-2 issue is nominated for reopening.**

---

## IMPORTANT — correct, or reject explicitly with reasons

**Retraction (why-layers).** An earlier draft of this challenge carried a sixth IMPORTANT for `b3-why-layers` F1–F2, written when only the parent was on disk. **It is retracted.** `b3-why-layers-fidfix.svg` exists and I verified from its SVG text that F1–F3 are already corrected: C021 sits outside the green `MEASURED on 1d04 · C001` well, the object prints `NOT this Fc-thiol 39-mer`, the `≠` operands are the three quantity types (`1d04 Kd_molecular ≠ AuED-MEA PBS EC50 ≠ tissue %gain`), and the red-X reads `different question / not next`. An independent F1–F3 check also returned PASS, with the object-3 `tissue faradaic identity` leftover correctly read as a **stamp, not a `≠` operand**. The parent is kept on disk and not overwritten; the sibling is the face to defend. The kill-test was **not** re-run on the sibling, so its SURVIVE status is inherited, not re-earned. Residual guard wording stays MINOR (M8) and MINOR (M1). **No IMPORTANT was added in its place** — the list below is five items, and I5 is the pre-existing reproducibility item, renumbered from I6, unchanged in content.

### I1. "in vitro eye-cup" is an unsupported preparation descriptor — and it is on the hero

`hybrid-hero-v3-fidfix2.svg` heads the construct anchor with **"The object that went in (in vitro eye-cup)"**. The round-2 answers repeat it three times ("the faradaic current in the eye-cup", "the shank in the eye-cup", "Hu's eye-cup") — and those answers are already mouth-patched for Q1/Q2/Q14, so "eye-cup" survived a revision pass.

The freeze says *in vitro* **isolated mouse retina** (`state/current_thesis.md`; C031/C033 `system` = `in vitro mouse retina`; E045 `system` = `in vitro mouse retina after insertion`). A case-insensitive search of the repository at the frozen commit for `eyecup` / `eye-cup` / `eye cup` returns **zero matches** in any file — no claim row, no evidence row, no source note, no storyboard, no caption.

Eyecup and isolated retina are different preparations, and the difference is not cosmetic for this poster: an eyecup narrative implies outer-retina/RPE context, which is exactly the direction of the pool question the package correctly holds **UNKNOWN** ("UNKNOWN: photoreceptor pool"). Mission 3 may not introduce experimental detail the freeze does not carry, in either direction.

**Smallest correction:** print the freeze's own words — `in vitro isolated mouse retina` — on the hero anchor, and strike "eye-cup" from the kit and the round-2 answers. String swap; no science reopen; no new evidence.

### I2. `b2-clocks` merges a buffer-calibration clock with two tissue clocks, unlabeled

The face prints three equal-weight tokens under one green stamp: **`14 s scan` · `~1 min/point` · `10 min plateau`**, titled *Clocks of the scan, not a Glu assay*, stamped `MEASURED: protocol times`.

Two of those are in-tissue interrogation clocks (E045, E046/C031, `in vitro mouse retina`). The third is a **probe-calibration** clock: C028 is the ~10 min plateau **after a 10 nM glutamate step**, `system = probe calibration`, locator Fig. 6.6. The freeze keeps that qualifier attached, and the project's own accepted-figure language states the separation flatly: *calibration clocks ≠ tissue-recording clocks*. On the face, the qualifier is absent, and equal weighting actively asserts equivalence of context.

The round-2 attack graded this **MINOR** and routed the fix to the mouth ("point and say 'calibration after a 10 nM step, not retina'"). I preserve that disagreement and grade it **IMPORTANT**, on the package's own published standard: freeze-correct speech that exists only in the oral kit is a *memorized patch* (`hostile-hybrid-v3-fidfix2.md`, Method). A matrix/system equivalence that the graphic supplies should not be repaired by a sentence the student may not say. This is also the one collapse the constitution names twice — no property transfer across buffers/matrices, and keep quantity types distinct (an interrogation clock and a step-response plateau are not the same clock flavour).

**Smallest correction:** `10 min plateau (buffer calibration, 10 nM Glu step)` as one token string. Keep equal type weight, no bar lengths, no new clocks, no 1.2 ms. Do not redraw the SURVIVE face beyond that string.

### I3. Visible biochemistry does not hold on the scored set

Across all eight scored faces (text + aria-labels), searching for `aptamer`, `SELEX`, `ferrocene`/`Fc` reporter chemistry, `redox`, `electron`, `conformation`, `folding`, `selectiv*`, `aspartate`, `glutamine`, `GABA`, `serotonin`, `dopamine`:

- **`aptamer` appears zero times.** The only construct tokens anywhere are `Fc-thiol 39-mer` (hero header, 3Q card 4) and `39-mer`.
- No face explains how binding becomes current, or that recognition is a folded nucleic-acid interaction at all.
- **C032 appears on no face.** The selectivity panel — 100 nM Glu vs 10 µM serotonin, dopamine, tyrosine, lactate, with **Asp/Gln/GABA absent** — is the freeze's own named open figure of merit (Claim 10, unknown #4) and the most directly *molecular* reason tissue identity is unresolved. It is invisible on the board and appears only inside two spoken answers.
- The package teaches a five-stamp vocabulary (MEASURED / INFERRED / MODELED / UNKNOWN / PROPOSED) while the hero footer itself concedes `MODELED = supporting caption only`. **No scored face instantiates MODELED.** A student asked to point at a modeled object has nothing to point at.

The recognition-first panel was correctly killed for U1 gravity, but nothing replaced it, so molecular recognition now has no surviving face. The rubric weights BIOC1600 biochemical depth and critical insight at 15 + 15; a refusal-only board spends both on hygiene.

**Correction routes that stay inside the freeze and do not resurrect the killed object:**

1. **A C032 selectivity face.** MEASURED half: the four interferents that were run at 10 µM against 100 nM Glu, no ratio printed (C032 `value` is intentionally empty). EMPTY half: Asp / Gln / GABA cells, stamped UNKNOWN. This face carries **no Kd numeral at all**, so it cannot acquire the U1 gravity that killed recognition-first, and it converts "identity is unnamed" from a procedural refusal into a recognition-chemistry statement a first-year can defend. Appendix II g/l must stay unconverted; Ames must not be drawn as a demonstrated confounder.
2. **A design-level transduction schematic**, explicitly stamped as the construct's design and not a measured mechanism, using only what the freeze carries (Fc-thiol 39-mer, immobilized film, ACV interrogation). If no ledger row supports a mechanism step, that step must not be drawn — an honest two-object schematic beats an invented three-step cartoon.
3. If a quantitative biochemistry beat is wanted, the freeze's **supporting** items are the 1:1 Langmuir 81-fold identity (C027/C013, `computational illustration` / `review-supported`, with the unpublished Langmuir–Freundlich *n* caveat printed) and τ_eq = 1/(kon·*c* + koff) (C007 empty). Both must carry MODELED and an explicit flagship-NONE line; neither may become a title.

An explicit rejection is available and must be argued if taken: *this board's job is the evidence boundary, and recognition chemistry belongs in the talk*. That trade should be recorded, not left implicit.

### I4. No scored face states the poster's question or names the subject at title scale

The locked question — *What does Hu's retinal ACV result establish, and what does it not?* — appears on **no** face. The hero's top band is three stamp tiles; its largest type is `current`, `unnamed`, `+`, `→`. The subject only becomes glutamate in an 11 pt orange tile two thirds of the way down, and the word *retina* appears only inside the anchor line. The storyboard likewise has no title/question region (Panels 1–6, no Panel 0).

The glance PASSes are real, but they were scored against a contract that deliberately excluded subject matter ("measured a current twice; molecule unnamed"). A reader who arrives without that contract can traverse the hero and not learn what was being sensed or why it matters. That is a hierarchy and comprehension gap, not a fidelity one.

**Smallest correction:** a title/question band carrying the locked question verbatim plus one purpose line (aptamer-based glutamate sensing in retinal tissue; two regimes live on 3H). No new claim, no new number.

### I5. Rebuildable figures / reproducibility does not hold as scored

All eight faces are hand-authored SVG living in the agent store, with no generator, no build script, and no git provenance. The project's own instruction for accepted figures is the opposite: *Rebuild (do not hand-edit SVGs): `sh analysis/accepted/rebuild.sh`*. As of this scoring, the constitution's **rebuildable figures** criterion does not hold and REPRODUCIBILITY would be **FAIL**, not partial.

**Two honest routes.** Either (a) give the scored faces a generator or bring them under version control with the single Mission-3 content PR when the package coheres — I am not opening one; or (b) **explicitly reject** the criterion for this class with the reason on record: these are zero-data wireframes (no data pixels, no derived values), so rebuildability applies to data figures only. Route (b) is defensible, but it must be written down, because "Partial" in the RC checklist currently reads as *in progress* rather than *scoped out*.

---

## MINOR

| # | Face / file | Item | Recommendation |
|---|---|---|---|
| M1 | `b2-spikes` (×3), `b3-why-layers` | **"faradaic"** as a standing descriptor of the tissue current. I traced it: it is **not invented** — it appears at the frozen commit in `poster/storyboards/mission3.md` and `poster/figures/mission3_boundary_glyph.CAPTION.md`. It is absent from `state/current_thesis.md`, which says ACV current / signal-gain. Calling the tissue current faradaic asserts a redox electron-transfer origin and quietly closes the non-faradaic / interface-settling alternative the freeze deliberately leaves open. | Prefer `ACV current` or `measured current` on faces and in the mouth. Inherited wording; not a science reopen. |
| M2 | hero Leg A | The freeze's Leg A is a post-insertion `%gain` **rise** from a dashed baseline. The face prints only `current changed`, and the direction is absent in words as well as in shape. Under-claiming a measured direction is also a fidelity gap, and it reads as concealment to anyone who has read S066. | Print the direction as a **word** with its open cause (`current rose from the post-insertion baseline; cause UNKNOWN — settling untested`). Explicitly **not** a rise cartoon and **not** a reopening of the bound rise-vs-windows type loop. |
| M3 | `b3-3q-traps` header | Row subtitle is `wrong object · not this probe's Kd`, but two of the four cards are not Kd at all (0.3 pM `sensor_LOD`, 25 nM `biological_concentration_range`). The header invites the LOD-≡-Kd collapse the row exists to prevent. | `wrong object · wrong quantity type`. |
| M4 | `b3-3h-regimes`, kit Q9 | `~25 nM` carries `MEASURED: ambient in this prep` with no basis token. The stamp is freeze-licensed, but C012's basis (ambient by **tonic NMDAR current**, i.e. after efficacy scaling) is nowhere on the face or in the kit, so "how do you measure 25 nM ambient?" has no pointable answer. | Add the basis as a short caption token. Precision, not a stamp change. |
| M5 | `u2-close`, answers Q14 | *"If they die on both legs, the inference is strengthened."* The freeze states only the **falsifying** direction. Loss of current on a scrambled/binding-null probe shows aptamer dependence, and a pharmacological arm has its own interface liabilities; neither names the molecule by itself. | Mark that sentence as the design's intent, or add the residual-confound clause. Keep the If/If close (the round-2 fix). |
| M6 | `b2-maps` Ames card | `10 nM–10 µM` (C030/E044 `analytical_working_range`) sits unlabeled beside a `signal_gain` slope, on the face whose whole job is quantity hygiene. | Label the quantity type. |
| M7 | `b3-3q-traps`, `b2-clocks` | These two faces carry **no** `Gate REVISE · Flagship NONE · not group-final` / prototype rail; the other six do. Not a FINAL label — a provenance gap if a face travels alone. | Add the rail. |
| M8 | `b3-why-layers-fidfix` (sibling) | **F1–F3 all repaired**, verified from SVG text: C021 is now its own white chip outside the green `MEASURED on 1d04 · C001` well and the object prints `NOT this Fc-thiol 39-mer`; the mechanism operands are now the three quantity types (`1d04 Kd_molecular ≠ AuED-MEA PBS EC50 ≠ tissue %gain`), so `identity` is no longer a `≠` operand; the red-X reads `U1 isotherm ≠ identity · different question / not next`. Two residuals: the face still has **no** `not "not glutamate"` guard (hero and `b2-spikes` both do), and object 3's layer subtitle is still `tissue faradaic identity` (see M1). | Defend the **sibling**, not the parent. Add the one-line not-negation guard; retitle the subtitle with M1. Do not reopen C001/C021; do not draw U2 homework here. |
| M9 | hero vs `b2-spikes` | Hero says `GCL/IPL border`; `b2-spikes` says `GCL`. Both are licensed (C034 says GCL; thesis prose says GCL/IPL border), but the split invites "which is it?" at the board. | Use one form on both, with the other as the caption gloss. |

---

## STYLE

- **`Glu` is never expanded on `b2-maps`, `b2-clocks`, or `b3-3q-traps`.** Aria-labels spell glutamate; the visible text does not. Expand once per face for a first-year reader.
- **A `≠` token does live on the hero** (`Same shank ≠ same measurand`), while the bind is written as "do not draw ≠ on the hero". The bind clearly means the finding row, and the shank line is correct as written; restate the bind as *no ≠ in the finding row* so a future editor does not "fix" the wrong glyph.
- `b3-3h-regimes` prints quantity-type chips for both concentrations but none for `decay τ ~1.2 ms` (stored as a kinetic inference under `response_time` elsewhere). Symmetry would help.
- Type floor: a 6.5 pt grey *p* tspan and 7–7.5 pt INFERRED chips are the smallest legibility bets on the hero. I scored SVG text only and did **not** render, so this is flagged as *untested at viewing distance*, not as a defect.
- Mixed font stacks (`sans-serif` vs `system-ui,sans-serif` on 3Q) and two card grammars inside the B3 row.

---

## Release criteria — what actually holds

| Criterion | Holds? | Basis |
|---|---|---|
| Freeze integrity | **Yes** | No fatal contradiction; every on-face numeral traced to a ledger row; no Mission-2 return item nominated |
| Correct main-claim comprehension | **Partial** | Glance/90 s/3 min PASS on a contract that excludes the subject and the question (I4) |
| Auditable evidence chains | **Partial** | why-layers F1–F3 repaired on the fidfix sibling (M8); still M7 (two faces with no status rail) and M9 (GCL wording split) |
| Consistent quantities / units | **Partial** | M3 header, M6 unlabeled working range; I2 merges two clock contexts |
| Rebuildable figures | **No** | I5: hand-authored SVG, no generator, no git provenance |
| Effective hierarchy | **Partial** | I4 no title/question object; 1.8 nM remains the only finding-band numeral (bound) |
| Visible biochemistry | **No** | I3: zero `aptamer`, no recognition/transduction, C032 absent, MODELED stamp with no instance |
| No systematic novice error | **Partial** | Novice walks PASS; I1 teaches a wrong preparation and I2 teaches a wrong clock context |
| Measurement vs inference distinguishable | **Yes** | Stamps are correct and on-well; quote/*p* bound MINOR; I5/F2 is audibility, not a stamp error |
| Visible uncertainty | **Yes** | UNKNOWN-on-well, empty hatched wells, empty tissue transfer |
| U2 follows from the gap | **Yes** | `u2-close` PROPOSED and empty; U1 refused as substitute on three faces |
| Defensible oral answers | **Partial** | Round-2 STOP with three mouth fixes; I1 wording and M5 asymmetry are in the mouth |

**INTERNAL RC: none.** **READY FOR EXTERNAL ASTRA AUDIT: no.** Gate **REVISE**. Not group-final. Not FINAL.

---

## Method, negative-search integrity, and wildcard

**What I actually did.** Read the constitution and the frozen `state/current_thesis.md` / `state/high_value_unknowns.md` first, then the eight faces as raw SVG text, then the oral kit, the round-2 answers, and the round-2 attack. Verified on-face numerals and locators against `state/claims.csv` and `research/evidence/core_evidence.csv` at the frozen commit rather than against other agents' summaries (C001, C005, C007, C011, C012, C021, C028, C029, C030, C031, C032, C033, C034; E044–E048). Ran targeted repository searches for `eyecup|eye-cup|eye cup` (0 matches, → I1) and `faradaic` (2 matches, both in frozen Mission-3-facing files, → M1 downgraded from a suspected invention to inherited wording). Ran a token sweep of all eight faces for recognition/transduction/selectivity vocabulary (→ I3). No renderer, no layout vote, no ledger edit, no prototype overwrite.

**Where I disagree with an existing grade.** `b2-clocks`' 10-minute token: round-2 attack **MINOR** with a spoken fix; this challenge **IMPORTANT** with an on-face string, because the package's own method rejects oral-only repairs for misreads the graphic supplies. Recorded rather than resolved by fiat.

**Wildcard direction examined.** I tested the opposite failure mode from the one everyone has been hunting: whether the triad *MEASURED current / INFERRED glutamate / UNKNOWN molecule* **under**-claims — i.e. whether Hu holds positive evidence of aptamer-dependent recognition (buffer selectivity against four interferents; matrix-dependent slopes) that the board is suppressing into a bare UNKNOWN. Result: the triad itself is correct and must not move — the freeze fixes tissue identity as `unresolved` and forbids the transfer that would upgrade it. But the search returned something useful anyway: the one piece of *positive* recognition evidence that does exist (C032's non-empty half) is on no face, which is independent support for I3 rather than a reason to soften the triad. Recorded as examined and partially productive, not manufactured.

**Not done, and why.** No new literature search (out of scope and forbidden as an end in itself). No renderer hunt (instructed). No re-run of bound type loops on quote/*p*, 1.8 nM placement, rise-vs-windows, or the `≠`-on-hero finding row (bound; re-asking them is a type loop, as the round-2 attack established). No prototype written or overwritten by this challenge — the why-layers fidfix sibling arrived from its own owner mid-pass and was scored, not edited.

**Is another challenge round likely to change a decision?** No. A third pass over these eight faces would reproduce the bound list. What would change a decision is one of the I3 routes (a C032 selectivity face or a design-level transduction schematic) plus the I1/I2 string corrections — i.e. an addition to the board, not another attack on it.
